package main

import (
	"fmt"
	col "gopackages/color"
	. "gopackages/wordz"

	"github.com/fatih/color"
)

func main() {
	col.Greet()
	color.Red("Hello world")
	for i := 0; i < 5; i++ {
		fmt.Println(Hello)
		fmt.Println(Random())

	}
}
